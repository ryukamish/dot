# X11 Profile and Initialisation Files

## What do they do?

Well, they are settings for X11 utility used to start `x` which is the display server in Linux. It probably does something with `GUI` programs.
